extends layout

block content
    h1= title
    p Welcome to #{title}
    form(action="", method="post", role="form")
        button.btn.btn-default(name="foo", value="changeUserType") Change user type